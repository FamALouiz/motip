[project]
name = "motip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cotengra>=0.7.5",
    "pyrefly>=0.54.0",
    "pytest>=9.0.2",
    "ruff>=0.15.4",
]

[tool.ruff]
line-length = 100

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
